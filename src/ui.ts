import { Application, Text } from "pixi.js";

export function draw(app: Application) {
    const text = new Text("SHAPES_01");
    text.position.set(app.view.width * 0.025, app.view.height * 0.05);

    // containers
    // let mainArea
    // app.stage.addChild(text);
}